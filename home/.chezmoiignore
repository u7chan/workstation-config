{{- /*
Piのキーバインド・modelOverrides・web-search.jsonはbootstrapがPiを選択した場合だけ配置する。
bootstrapはWORKSTATION_PI_SELECTED=trueを渡し、未選択時・変数未設定時は
これらのファイルをchezmoi管理対象外にする。既存ファイルの削除はbootstrapが行う。
web-search.jsonはファイル名のcreate_プレフィックス（home/dot_pi/create_web-search.json）で
create属性を持つため、選択時でも既存ファイル（API key等の手動追加）は上書きしない。
※パターンはsource内のdot_piではなく、ターゲット側の.piに一致させる。
*/}}
{{- if ne (env "WORKSTATION_PI_SELECTED") "true" }}
.pi/agent/keybindings.json
.pi/agent/models.json
.pi/web-search.json
{{- end }}
README.md
